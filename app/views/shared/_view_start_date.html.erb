(<%= object.start_date.present? ? object.start_date.strftime(
  t("shared.datetime_format")) : t("shared.not_availble") %> -
<%= object.end_date.present? ? object.end_date.strftime(
  t("shared.datetime_format")) : t("shared.not_availble") %>)
